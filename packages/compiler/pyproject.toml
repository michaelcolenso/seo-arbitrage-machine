[project]
name = "datasiteforge-compiler"
version = "0.1.0"
description = "DataSiteForge compiler: hydrates approved evaluations into Astro sites."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasiteforge-core",
    "datasiteforge-engine",
    "pydantic>=2.6",
    "typer>=0.12",
    "rich>=13.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dsf_compiler"]
